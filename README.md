React Native SDK that's built by managing React Native via native Android/iOS. 

This has proven to be difficult because of video-client, media-soup use. Use react-native-sdk instead https://github.com/nativeframe/react-native-sdk
